[project]
name = "medops-engine"
version = "0.1.0"
description = "Fault-scenario engine for medops device simulators."
requires-python = ">=3.11,<3.13"
dependencies = [
    "medops-common",
    "pydicom>=3.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/medops_engine"]
