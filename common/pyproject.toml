[project]
name = "medops-common"
version = "0.1.0"
description = "Shared constants, utilities and schemas for medops."
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/medops_common"]
